# Ideapick — спецификация

**Продукт:** путь от идеи до решения «делать / не делать» для разработчиков и соло-предпринимателей.  
**Домен:** ideapick.ru · **Язык UI:** русский · **Принцип:** минимум ввода от пользователя, максимум выводов от системы.

```
Идея (своя / каталог / AI по анамнезу) → анализ (AI) → [сравнение] → вердикт (статус)
```

---

## Маршруты

| Путь | Кто | Суть |
|------|-----|------|
| `/` | все | Лендинг, цены, CTA «Начать», **+100 ₽ при регистрации** |
| `/login` | гость | Google или magic link → `/app/ideas` |
| `/app/ideas` | auth | Список (grid), фильтр/сортировка, баланс; добавить идею, рандом, анамнез — в UI |
| `/app/ideas/[id]` | auth | Карточка: редактирование, анализ, статус, сравнение, архив |
| `/app/compare` | auth | 2–4 идеи с анализом, таблица без нового AI |
| `/app/settings` | auth | Аккаунт, баланс, пополнение, история, удаление аккаунта; return после ЮKassa |
| `/privacy`, `/terms` | все | Политики; оплата — **самозанятость** (НПД), чеки через ЮKassa |

`/app/*` без сессии → `/login?callbackUrl=...`

---

## Идея

**Ввод:** название (≤100), описание + placeholder-подсказка. Аудиторию, нишу, монетизацию не спрашиваем.

**Статус:** одно поле на идею. После анализа система может выставить статус по рекомендации AI; пользователь меняет вручную. Без «согласен / не согласен». Архив скрывает идею из активного списка. Конкретные значения и переходы — в разработке.

---

## Генерация

| Режим | Цена | Механика |
|-------|------|----------|
| **Случайная** | 0 ₽ | Выдача из каталога, без AI. **3 раза в сутки** на пользователя. UI: «из каталога», не «нейросеть». |
| **По анамнезу** | 49 ₽ | Краткий опрос → AI → несколько карточек-идей. |

**Каталог:** выданная идея этому пользователю не повторяется. Исчерпан пул или дневной лимит → кнопка недоступна. Пополнение каталога — вручную бэкендом, UI админки нет.

---

## Анализ (AI)

**99 ₽** · вход: название + описание · ответ — **структурированный JSON** (не простыня).

Пересчёт: замена отчёта с подтверждением. Дисклеймер: оценка AI, не совет и не гарантия.

**Надёжность:** ретраи; hold на балансе → списание или возврат (см. монетизацию). UI: состояние загрузки.

---

## Сравнение

2–4 идеи с готовым анализом · **0 ₽** · без нового вызова AI.

Таблица собирается из сохранённого JSON анализа. Отдельный AI-текст «победитель сравнения» — вне scope.

---

## Монетизация

Баланс ₽, без подписки. **ЮKassa**. Мин. пополнение **100 ₽** (пресеты 100/300/500/1000). Приём: **самозанятость** (при росте — ИП).

| Событие | Сумма |
|---------|--------|
| Первая регистрация | **+100 ₽** |
| Анализ / пересчёт | 99 ₽ |
| Генерация по анамнезу | 49 ₽ |
| Рандом, сравнение, CRUD | 0 ₽ |

**Hold:** сумма блокируется → AI → успех: списание · провал после ретраев: возврат. Доступный баланс = общий − активные hold.

Перед платной операцией: цена + подтверждение. История операций (пополнения и списания) — в настройках.

---

## Авторизация

**Better Auth** · без паролей · **без GitHub** (нет надёжно подтверждённой почты без отдельной верификации).

Google — основной; magic link — альтернатива. Один аккаунт на email (Google + почта). Первый вход = регистрация + **100 ₽**.

---

## Нефункциональное

Rate limit (auth, AI, рандом 3/сутки). HTTPS. Удаление аккаунта — каскадно. Деплой: VPS, Docker, Next standalone.

---

## Стек

Next.js (App Router) · TypeScript · Tailwind · PostgreSQL · Drizzle · AI API (DeepSeek) · Better Auth · ЮKassa

---

## Вне scope

GitHub OAuth · админка · команды · PWA · публичный шаринг · глобальный профиль · passkeys · подписка · вид «таблица» · AI-итог сравнения
